---
import Base from "../layouts/Base.astro";
import { copy } from "../lib/content";
const en = copy.en;
const es = copy.es;
---

<Base page="legal">
  <div class="mx-auto max-w-2xl px-5 pt-28 pb-20 sm:px-8 sm:pt-32">
    <h1 class="font-display text-section font-medium italic text-ink" data-en={en.legal.title} data-es={es.legal.title}>
      {en.legal.title}
    </h1>
    <p class="mt-8 text-lead leading-relaxed text-ink-soft" data-en={en.legal.body} data-es={es.legal.body}>
      {en.legal.body}
    </p>
  </div>
</Base>
